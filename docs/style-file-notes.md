# Style file notes

`course.sty` is the place for shared commands, fonts, margins, and repeated layout choices.

I keep package changes in that file instead of loading packages inside question files. That makes copied homework folders easier to compare.

A style edit needs a PDF build, even when the changed line looks small.
