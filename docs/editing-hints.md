# Editing hints

Hints should explain the path to the answer without hiding the question source.

When I change a hint, I check these points:

1. The hint file has the same number as the matching question when possible.
2. The math notation builds with the current packages.
3. The full template builds after the change.

I avoid long hidden solutions inside question files.
