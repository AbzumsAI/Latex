# Math notation

The style file already defines common sets such as `\IN`, `\IZ`, `\IQ`, `\IR`, and `\IC`.

I prefer these commands in question files because they keep notation steady across assignments.

When a symbol needs a new package, I add the package to `course.sty` and build the sample PDF.
