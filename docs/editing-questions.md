# Editing questions

When I add a question, I start with a new file in `Template\questions`.

Then I add a matching `\مسئله` line and `\input` line in `HW.tex`. This keeps the title and body close in the main file.

After the edit, I build the PDF and check the page where the question appears.
