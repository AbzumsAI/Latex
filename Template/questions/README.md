# Question files

Each file in this folder holds one question or one short note that belongs with the assignment.

I keep questions in separate files so `HW.tex` stays easy to read. When I add a new file here, I also add the matching `\input` line in `HW.tex`.

A question file should not load packages. Shared packages belong in `commons\course.sty`.
