# Template folder

I use this folder as the copy point when a new homework sheet starts.

`HW.tex` is the entry file. It loads the course style, then imports question files from `questions`.

Before I copy the template into another repo, I build it once and check the first page of the PDF.
