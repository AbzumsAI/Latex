# Answer hints

The files in this folder hold answer hints that can be included by a course sheet when needed.

I keep hints separate from question text because some PDFs should show only problems. When I edit a hint, I build the full template instead of checking the hint alone.

Simple math commands are safer than adding a package for one line of notation.
